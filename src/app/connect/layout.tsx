export const metadata = {
  title: 'Connect',
  description:
    'Get in touch with the Foundrie AI team — for founders, professionals, programs, and partners.',
};

export default function ConnectLayout({ children }: { children: React.ReactNode }) {
  return children;
}
