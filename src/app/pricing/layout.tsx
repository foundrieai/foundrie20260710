export const metadata = {
  title: 'Pricing',
  description:
    'Simple, transparent pricing for individuals, teams, and programs. Start free and scale into the plan that matches your ambition.',
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
