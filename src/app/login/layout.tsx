export const metadata = {
  title: 'Sign In',
  description: 'Log in to access your Foundrie AI dashboard, reports, and tools.',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
