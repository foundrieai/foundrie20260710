export const metadata = {
  title: 'Resources',
  description:
    'Insights, tools, and guides to help you succeed at every stage of your journey in the AI era of business.',
};

export default function ResourcesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
